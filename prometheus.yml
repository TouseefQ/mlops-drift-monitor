global:
  scrape_interval: 5s # Scrape every 5 seconds (fast for testing)

scrape_configs:
  - job_name: 'drift-monitor'
    static_configs:
      # CRITICAL: We use 'drift-app' here, not localhost.
      # Docker Compose lets containers talk by name.
      - targets: ['drift-app:8000']